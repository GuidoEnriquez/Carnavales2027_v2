import { getPool } from "../../db/pool.js";
const text=(v,n)=>{if(typeof v!=="string"||!v.trim())throw new TypeError(`${n} debe ser texto no vacío.`);return v.trim();};
export async function createRubric({client=getPool(),eventId,name,code,evaluationTarget,expectedSubjectType=null}){const {rows}=await client.query(`INSERT INTO rubric(event_id,name,code,evaluation_target,expected_subject_type) VALUES($1,$2,$3,$4,$5) RETURNING id,event_id AS "eventId",name,code,evaluation_target AS "evaluationTarget",expected_subject_type AS "expectedSubjectType",active`,[text(eventId,'eventId'),text(name,'name'),text(code,'code'),text(evaluationTarget,'evaluationTarget'),expectedSubjectType]);return rows[0];}
export async function createItem({client=getPool(),rubricId,name,code,specialtyId}){const {rows:r}=await client.query('SELECT event_id FROM rubric WHERE id=$1',[text(rubricId,'rubricId')]);if(!r[0])throw new TypeError('rubricId inválido.');const {rows}=await client.query(`INSERT INTO evaluation_item(event_id,rubric_id,specialty_id,name,code) VALUES($1,$2,$3,$4,$5) RETURNING id,rubric_id AS "rubricId",specialty_id AS "specialtyId",name,code,active`,[r[0].event_id,rubricId,text(specialtyId,'specialtyId'),text(name,'name'),text(code,'code')]);return rows[0];}
export async function getRubric({client=getPool(),rubricId}){const {rows}=await client.query(`SELECT r.id,r.name,r.code,r.evaluation_target AS "evaluationTarget",COALESCE(json_agg(DISTINCT jsonb_build_object('id',s.id,'code',s.code,'name',s.name)) FILTER(WHERE s.id IS NOT NULL),'[]') AS specialties FROM rubric r LEFT JOIN evaluation_item i ON i.rubric_id=r.id AND i.active LEFT JOIN event_specialty s ON s.id=i.specialty_id AND s.active WHERE r.id=$1 GROUP BY r.id`,[text(rubricId,'rubricId')]);return rows[0]??null;}
